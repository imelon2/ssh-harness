{
  "name": "ssh-harness",
  "version": "1.0.4",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node sh/build.mjs",
    "start": "node bridge/server.js",
    "dev": "tsx src/server.ts",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"skip lint placeholder\" && exit 0",
    "version": "bash sh/version.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "yaml": "^2.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "esbuild": "^0.28.0",
    "fast-check": "^3.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
